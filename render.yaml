services:
  - type: worker
    name: lz-tbot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python lz_tbot.py --symbol R_75 --stake 0.35
    envVars:
      - key: DERIV_TOKEN
        sync: false
      - key: PYTHONUNBUFFERED
        value: 1
